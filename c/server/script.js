// 示例JavaScript文件
console.log('C语言HTTP服务器正在运行！');

// 添加点击计数器
let clickCount = 0;
document.addEventListener('click', function() {
    clickCount++;
    console.log('页面被点击了 ' + clickCount + ' 次');
});
